# SOS- — 团长春日 Codex Pet

Codex 自定义宠物，基于凉宫春日的活力与团长气场设计，并加入了微小的电路感发夹细节。

安装时将仓库根目录中的 `pet.json` 与 `spritesheet.webp` 一起复制到 Codex 的自定义宠物目录。`pet.json` 已声明 `spriteVersionNumber: 2`。

发布内容仅包含安装所需的清单与精灵图；本地校验资料不上传，以免携带环境路径信息。
